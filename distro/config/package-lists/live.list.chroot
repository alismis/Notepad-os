# Live system tooling
live-boot
live-config
live-config-systemd
# Required so live-config creates the live user account (a live-config
# Recommends, which is dropped because recommends are disabled).
user-setup

# Firmware & kernel
linux-image-amd64
firmware-linux-free

# Base utilities
sudo
locales
dbus-x11
xdg-utils
